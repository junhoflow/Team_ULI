<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whale">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/whale_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/whale_app_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Whale">
        <activity android:name=".ProfileInfoActivity"></activity>
        <activity android:name=".LeaderActivity">
        </activity>
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".termandregulations"></activity>
        <activity android:name=".ForLoading2"></activity>
        <activity android:name=".ForLoading" />
        <activity android:name=".ActivityJoinMembership" />
        <activity android:name=".FollowerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>